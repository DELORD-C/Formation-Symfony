{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bienvenue !</h1>
    <a href="{{ path('app_date_now') }}">Date</a>
    {{ html|raw }}
    <i class="bi bi-0-circle"></i>
    <img src="{{ asset('build/images/image (1).jpg') }}">
{% endblock %}