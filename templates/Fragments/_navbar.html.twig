<nav>
    <a href="{{ path('app_default_index') }}"><img src="{{ asset('build/medias/image.png') }}"></a>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <a href="{{ path('app_post_list') }}">Posts</a>
        <a href="{{ path('app_user_logout') }}">Logout</a>
    {% else %}
        <a href="{{ path('app_user_login') }}">Login</a>
        <a href="{{ path('app_user_register') }}">Register</a>
    {% endif %}
</nav>