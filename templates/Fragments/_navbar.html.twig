<nav class="navbar navbar-expand-lg p-2 bg-opacity-10 bg-black">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_default_index') }}">
            {% if is_granted('IS_AUTHENTICATED_FULLY') and app.user.pic %}
                <img src="{{ asset(upload_path ~ '/pics/' ~ app.user.pic) }}">
            {% else %}
                <img src="{{ asset('build/medias/logo.svg') }}">
            {% endif %}
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if is_granted('ROLE_SUPERADMIN') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Users
                    </a>
                    <ul class="dropdown-menu">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_admin_create') }}">Create</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_admin_list') }}">List</a>
                        </li>
                    </ul>
                </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Posts
                    </a>
                    <ul class="dropdown-menu">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_post_create') }}">Create</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_post_list') }}">List</a>
                        </li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_logout') }}">{{ 'Logout'|trans }}</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_login') }}">{{ 'Login'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_register') }}">Register</a>
                </li>
                {% endif %}

                {% include 'Fragments/_locale.html.twig' %}
            </ul>
            <div class="d-flex justify-center align-items-center gap-2"><span class="d-flex justify-center align-items-center" id="city"><img src="{{ asset('build/medias/loading.gif') }}"></span> | <span id="meteo" class="d-flex justify-center align-items-center"><img src="{{ asset('build/medias/loading.gif') }}"></span></div>
{#            <form class="d-flex" role="search">#}
{#                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">#}
{#                <button class="btn btn-outline-success" type="submit">Search</button>#}
{#            </form>#}
        </div>
    </div>
</nav>